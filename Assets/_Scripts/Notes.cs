using UnityEngine;

public class Notes : MonoBehaviour
{
    // You can change the movement from MoveTowards to addForce type ( for sharper turns if you want to go left while going right or vice versa)
    // Or you can do this with wants to turn boolean or so
     
    // Air control will be added
    // Portals will be added ( they may be used to change the scene later on )
    // On teleport system sometimes entering portal from above ( with velocity ) was triggering multiple teleports back and forth. Setting velocity to zero after teleporting fixed it. Find out why it fixed it.

}
